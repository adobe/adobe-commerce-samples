{
  "meshConfig": {
    "sources": [
      {
        "name": "CommerceGraphql",
        "handler": {
          "graphql": {
            "endpoint": "{{env.COMMERCE_GRAPHQL_ENDPOINT}}"
          }
        }
      },
      {
        "name": "CommerceREST",
        "handler": {
          "openapi": {
            "source": "{{env.COMMERCE_REST_ENDPOINT}}"
          }
        }
      }
    ]
  }
}
